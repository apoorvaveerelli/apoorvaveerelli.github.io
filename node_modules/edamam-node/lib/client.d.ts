import { SearchHits, SearchOptions } from "./types";
export interface EdamamParams {
    appId: string;
    appKey: string;
    baseUrl?: string;
}
export declare class EdamamClient {
    private appId;
    private appKey;
    private baseUrl;
    constructor(params: EdamamParams);
    searchRecipesByQuery(q: string, options?: SearchOptions): Promise<SearchHits>;
    searchRecipesById(r: string, options?: SearchOptions): Promise<SearchHits>;
    private searchRecipes;
}
